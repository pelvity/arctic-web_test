{
  "name": "arctic-web_test",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "prepare": "husky",
    "dev": "npm run dev -w frontend & npm run start:dev -w backend",
    "start": "npm run start -w frontend & npm run start -w backend",
    "lint": "npm run lint -w frontend && npm run lint -w backend",
    "test": "npm run test -w backend",
    "test:e2e": "npm run test:e2e -w backend && npm run test -w frontend"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.0.0"
  },
  "lint-staged": {
    "frontend/**/*.{js,jsx,ts,tsx}": [
      "npm run lint -w frontend",
      "prettier --write"
    ],
    "backend/**/*.{js,ts}": [
      "npm run lint -w backend",
      "prettier --write"
    ]
  }
}